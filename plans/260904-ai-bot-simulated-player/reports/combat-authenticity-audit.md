# Combat authenticity audit — Horse / Range / PK

Date: 2026-09-04
HEAD: pending commit

## A. HorseLimit semantics (THIS server)

Verified from `settings/skills.txt`:

| HorseLimit | Count | Meaning (authoritative for this repo) |
|------------|-------|----------------------------------------|
| 0 | 989 | **MUST dismount** before cast (default deny) |
| 1 | 235 | **Allowlist**: may cast while mounted |

Evidence:

- Majority of skills are HL=0; horseback combat skills are the minority (fits VLTK design).
- Known melee/ranged samples with HL=1 include 10, 17, 30, 318, 323, 328, 336, 342, …
- Known foot-primary samples with HL=0 include 302, 321, 322, 361.

Runtime policy: `SimSkillMeta:CanCastOnHorse(id)` → 1 only if HL>=1; **everything else → 0**.

## B. AttackRadius

- Field `AttackRadius` in `skills.txt` is **pixels**.
- Tiles = `max(1, floor(AR/32))`; AR=0 → failsafe **2 tiles** (not 90px silent ranged).
- Cached once in `sim.skill_meta.lua` (generated by `tools/gen_skill_meta.py`).

## C. Duplicate systems

| Old | Status |
|-----|--------|
| `HORSE_SKILLS` / `sim.horse_skills.lua` | Superseded by `SimSkillMeta` |
| `SIMBOT_DISMOUNT_SKILLS` | Emptied; not used for mount policy |
| `SKILL_ATTACK_RADIUS` | Fallback only if meta missing |
| `SIMBOT_SKILL_RANGE` / `SIMBOT_MELEE_*` | Fallback only |

Authoritative runtime: **SimSkillMeta**.

## D. pendingSkill pipeline

`SimPickSkill` sets `tbNpc.pendingSkill` / `pendingSkillId` and **does not** flip `bua2Toggle` until `SimCommitSkillToggle` after cast.

`SimBotCastDist(tbNpc, skillId)` takes the same skill id used for horse + cast.

## E. PK root cause

Train/combat SimBots could become **NpcKind=4** (NPC / unattackable) via `SetFightState` / follow-parent paths, while still showing hostile camp colors.

Fix:

- `mode=="train"` / `tongkim` / `isAttackable==1` → **always `SetNpcKind(0)`**
- `isAttackable=1` default for train/TK
- `SimEnsureCombatAttackable` on breath/duel

Player can PK eligible combat bots in non-peace maps; peaceful bots still do not **initiate** aggro (`CHANCE_ATTACK_PLAYER=0` unless DoSat). DoSat restored at `TRAIN_DOSAT_PCT=8`.

## F. Tests

- Automated: `tests/test_sim_skill_meta.py`, existing train combat tests
- Runtime (requires GS restart by user): horse cast, melee approach, player Đồ sát hits train bot
